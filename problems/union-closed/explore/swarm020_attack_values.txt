# one task per line; {{value}} in the attack template
TASK 1 (kill geometry for MM_abs). MM_abs is a NONNEGATIVELY-weighted mean of (log2 OR - lambda), so killing it needs histories with log2 OR < lambda at HEALTHY margins (x, y well inside (0,1), so |sigma| is large) carrying enough mass to beat the surpluses. Design, with explicit numbers, a small configuration of histories (masses m_j, margins (x_j, y_j), realized z~_j in the Frechet range) that makes MM_abs as negative as possible while every history is realizable from a measure mu with element-marginals <= 0.38271 (you may treat realizability loosely, but say what family-of-sets structure — blocks, mirrored blocks, nested slices — would produce deficit at healthy margins; the known deficit mechanisms all have near-degenerate margins). Then describe the family shape as a recipe a search could implement (number of atoms, which coordinates correlate, rough weights). If you believe no such configuration exists, argue why the deficit channel is FORCED to degenerate margins in-regime.
TASK 2 (floor proof attempt for MM_abs). Prove as much as you can of: MM_abs >= 0 (a) for supports of at most 4 atoms; (b) to first order in lambda at any fixed mu (is the first-order coefficient of MM_abs's numerator still a perfect square / nonnegative? note the weight |sigma_lambda| tends to a positive limit as lambda -> 0 at fixed nondegenerate margins, so first order should inherit F4 — make that exact); (c) for measures at TV distance <= delta from a product measure (state the delta your argument gives). Be explicit where the |sigma| weight breaks a step that worked unweighted.
TASK 3 (a1-degenerate families for the window kill). Using F4's cancellation structure: construct, or prove impossible, a family mu_n scaled with n, marginals <= 0.38271, such that the aggregated first-order coefficient a1(n) = O(1/n) or smaller (needing sum_a nu(a) D_a ~ 0 at every coordinate: prefix-signed cancellation of future-bias vectors) while the second-order coefficient a2(n) is negative and bounded away from 0. Give the construction as an explicit recipe (atoms, weights, how the cancellation is arranged, why a2 < 0 survives the cancellation). Note: exact products have a1 = 0 but also a2 = 0 (F2), so the game is to cancel a1 WITHOUT killing a2 — say precisely why your construction's a2 does not vanish with a1, or prove that a1 -> 0 forces a2 -> 0 under the marginal cap (that would be a major structural result for the window candidate).
TASK 4 (what does the assembly actually need). The chain-rule assembly consumes, at each history, the realized gain h2(z~) versus the target gain h2(z_{2^lambda}); the per-history difference (the secant form) is certified NEGATIVE on a known witness, so no history-pointwise gain parity can be demanded. Derive the weakest coordinate-level or lambda-INTEGRATED inequality that still makes a Gilmer-style entropy assembly close: e.g. is int_0^lambda E[sigma_s (log2 OR - s)] ds >= 0 sufficient, and does the integration smooth the F6 sign flip (the integrand at small s has z_{2^s} < 1/2 more often)? State the cleanest sufficient condition you can find and check it BY HAND on a two-history toy (one surplus history at z~ > 1/2, one deficit at z~ < 1/2) to show it genuinely tolerates per-history losses.
TASK 5 (no-go attempt for per-history weightings). Attempt to prove: there is NO measurable weight function w(x, y, lambda) >= 0, not identically 0, such that E[w * (log2 OR - lambda)] >= 0 for every mu with marginals <= 0.38271 AND w equals the chain-rule sensitivity |sigma_lambda| wherever sigma_lambda > 0 (the property needed for the bound to feed the entropy assembly). Strategy hint: the known witness has surplus histories at sigma < 0 and a deficit history at near-degenerate margins; a valid w must dampen the deficit (w -> 0 at degenerate margins) yet the free-support searches reach +0.0101 — determine whether the two requirements (assembly-compatibility, positivity on all mu) are jointly satisfiable, and if you cannot decide, state exactly what extremal mu would separate them.
TASK 6 (transfer from dependence/copula theory). The Plackett copula family and log-odds-ratio functionals are classical statistics objects. List every theorem you know from copula/dependence theory potentially relevant to bounding mass-weighted averages of (log2 OR - lambda) over conditional 2x2 tables of a coupled pair: monotonicity/TP2 properties of the Plackett family in rho, stochastic orderings in (x, y), convexity of h2(z_rho) in log rho, Frechet-bound behavior, known variational characterizations. For each: exact statement, source (author/year as best you recall — flag uncertain citations), and one sentence on how it could bear on F6/F7. Honest blanks beat invented citations.
