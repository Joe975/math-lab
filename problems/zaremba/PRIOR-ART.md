# Zaremba — prior art

> **Tier 1.** What this lab has tried. Index in `prior-art.json`; full records
> in `attempts/`.

Nothing attempted yet. The problem was added 2026-07-27; the natural first
cycle is a baseline census: verify z(n) ≤ 5 over a large range with witnesses,
compute exact z(n) on a smaller range, and cross-check with an independent
generation-based implementation, per the contract in `PROBLEM.md`.
